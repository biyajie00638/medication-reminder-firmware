# 发布说明 · 用药提醒固件

## v1.1.0 — 量产就绪（2026-08-06）

面向「有人订购、需要操作多台」的固件侧收尾。

### ✨ 新增 / 改进
- **MAC 运行时自动读取**：开机读取硬件 MAC（`WiFi.macAddress()`），`config.h` 的 `DEVICE_MAC` 改为**可选**。
  → 一份 `firmware.bin` 可烧录所有设备，量产无需逐台改 MAC。
- 配合服务端「设备机群」视图：设备首次联网即**自动注册**（名称初始=MAC），运营方后台改名即可。

### 🔧 配套服务端（v1.0.0）
- 后台「设备机群」表格：在线/离线、固件版本、今日依从率、提醒数、家属提醒标记，可搜索、可改名、可下钻
- 设备改名接口 `PUT /api/devices/:mac`

### ⚠️ 升级注意
- 升级到 v1.1.0 后，旧设备若 `config.h` 写死 `DEVICE_MAC`，仍以写死值为准（宏仍生效）；
  新烧录设备建议留空以启用自动读取。
- OTA 双分区保护：刷写失败自动回滚，不会变砖。

### 🗺️ Roadmap
- 设备端显示本机 MAC / 绑定状态，便于无屏排查
- 缺药提醒、提醒灵活配置（Snooze / 周末跳过）
- 多语言语音（方言 TTS）
